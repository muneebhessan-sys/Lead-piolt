# Database Report

The application has an Alembic baseline migration (`0001_foundation`) defining `businesses` and `search_jobs`. PostgreSQL and Docker engine were unavailable during the latest verification, so migrations, foreign-key behavior, indexes, and persistence could not be tested. This is a release blocker.
