# Local Intelligence

LeadPilot starts without an LLM. Its built-in Python `IntelligenceEngine` performs deterministic lead scoring and evidence-bound composition. It only uses supplied business/audit facts and has no cloud or model fallback.
