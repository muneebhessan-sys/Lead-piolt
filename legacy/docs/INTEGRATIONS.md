# Integrations

Only official APIs/OAuth are planned. Google Places credentials and Gmail OAuth are separate. No Google Maps scraping, unofficial WhatsApp automation, or simulated OAuth is used. Integration status must be tested against the provider before it may display `CONNECTED`.
